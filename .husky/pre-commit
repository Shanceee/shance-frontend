#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Проверяю ESLint..."
npm run lint

if [ $? -ne 0 ]; then
  echo "❌ ESLint проверка не прошла. Исправь ошибки перед коммитом."
  exit 1
fi

echo "🧪 Запускаю тесты..."
npm test

if [ $? -ne 0 ]; then
  echo "❌ Тесты не прошли. Исправь ошибки перед коммитом."
  exit 1
fi

echo "✅ Все проверки пройдены. Коммит разрешен."
